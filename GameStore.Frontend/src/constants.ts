export const API_BASE_URL = "/api";

export const GAMES_ENDPOINT = `${API_BASE_URL}/games`;
export const GENRES_ENDPOINT = `${API_BASE_URL}/genres`;

export const TABS: string[] = ["Home", "Games"];

export default {
  GAMES_ENDPOINT,
  GENRES_ENDPOINT,
  TABS,
};
